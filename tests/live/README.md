# Live model records

These records are optional network inputs. Network tests assert that the tool runs and reports
status cleanly; they do not assert that Hugging Face model status is stable forever.
